# Breakpoint Screenshots CLI (Bun + Playwright)

Take automatic screenshots at your design system breakpoints using Bun and Playwright.

## Requirements

- [Bun](https://bun.sh/) installed
- Node / npm available for Playwright's browser install step

## Install

```bash
bun install
npx playwright install chromium
# or: bunx playwright install chromium
