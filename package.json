{
  "name": "breakpoint-screenshots",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "screenshot": "bun run src/cli.ts",
    "build": "bun build src/cli.ts --outdir dist --target bun",
    "start": "bun run dist/cli.js"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
